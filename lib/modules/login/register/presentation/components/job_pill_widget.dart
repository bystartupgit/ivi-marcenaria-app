import 'package:flutter/material.dart';

class JobPillWidget extends StatelessWidget {
  const JobPillWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
