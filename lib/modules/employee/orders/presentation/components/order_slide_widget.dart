
import 'package:flutter/material.dart';

class OrderSlideWidget extends StatelessWidget {
  const OrderSlideWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
